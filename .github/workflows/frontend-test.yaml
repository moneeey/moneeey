name: frontend-test
on:
  push:
    paths: [ 'frontend' ]
jobs:
  frontend-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
      with:
        node-version: '16.17'
        cache: 'yarn'
    - run: yarn
    - run: yarn test
