{
	"tasks": {
		"dev": "deno run --allow-read --allow-env --allow-net --allow-import --watch main.ts",
		"test": "deno test --allow-read --allow-env --allow-import",
		"test:update-snapshot": "deno test --allow-all -- --update",
		"test:watch": "deno test --allow-read --allow-env --allow-import --watch",
		"test:coverage": "rm -rf /tmp/coverage && deno test --allow-read --allow-env --allow-import --coverage=/tmp/coverage && deno coverage --exclude=generate_key.ts --exclude=config.ts /tmp/coverage"
	}
}
